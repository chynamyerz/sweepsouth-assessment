import React, { FC } from 'react';
import Grid from '@material-ui/core/Grid';

import { ProfileProps } from './types';

export const Profile: FC<ProfileProps> = () => {
  return <Grid>Pofile image and profile summary goes here</Grid>;
};
