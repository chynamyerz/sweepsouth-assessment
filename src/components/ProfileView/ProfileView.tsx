import { FC } from 'react';
import Grid from '@material-ui/core/Grid';

import { ProfileViewProps } from './types';

export const ProfileView: FC<ProfileViewProps> = () => {
  return <Grid>View the profile goes here</Grid>;
};
